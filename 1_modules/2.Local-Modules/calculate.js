function add(a, b) {
  return a + b;
}

function subtraction(a, b) {
  return a - b;
}

// common js modules

// module.exports = add;

// es modules multiple file exporting

export { add, subtraction };

// es modules  default exporting

// export default add